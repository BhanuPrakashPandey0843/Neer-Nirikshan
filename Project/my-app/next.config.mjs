/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "www.wonderskool.com",
      "1.bp.blogspot.com",
      "global-uploads.webflow.com", 
    ],
  },
};

export default nextConfig;
